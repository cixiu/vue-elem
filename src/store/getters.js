export const selectedShopper = state => state.selectedShopper;

export const cartFoods = state => state.cartFoods;

export const selectedEntries = state => state.selectedEntries;

export const latitude = state => state.latitude;

export const longitude = state => state.longitude;

export const geohash = state => state.geohash;
